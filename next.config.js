/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'lh3.googleusercontent.com',
            'localhost',
            'i.ndtvimg.com',
            'klike.net'
        ]
    }
}

module.exports = nextConfig
